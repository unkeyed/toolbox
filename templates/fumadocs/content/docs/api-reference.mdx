---
title: My API
toc: false
---

import { Root, API, APIInfo, APIExample, Responses, Response, ResponseTypes, ExampleResponse, TypeScriptResponse, Property, ObjectCollapsible, Requests, Request } from "fumadocs-ui/components/api";

<Root>

<API>

<APIInfo method={"GET"} route={"/keys/v1/create/"}>

| Status code | Description |
| ----------- | ----------- |
| `200` | Successful response when a key is created |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X GET "https://example.com/keys/v1/create/"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://example.com/keys/v1/create/", {
  method: "GET"
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "key": "1234567890",
  "keyId": "1234567890"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  /**
   * The key
   */
  key: string;
  /**
   * The keyId
   */
  keyId: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/posts/v1/all/"}>

### Header Parameters

<Property name={"authorization"} type={"string"} required={true} deprecated={false}>

<span>Example:  `"Bearer UNKEY_API_KEY"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Retrieve list of posts |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X GET "https://example.com/posts/v1/all/" \
  -H "authorization: Bearer UNKEY_API_KEY"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://example.com/posts/v1/all/", {
  method: "GET",
  headers: {
  "authorization": "Bearer UNKEY_API_KEY"
}
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "posts": [
    {
      "id": 0,
      "title": "string",
      "post": "string"
    }
  ]
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  posts: {
    id: number;
    title: string;
    post: string;
  }[];
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/posts/v1/create/"}>

### Request Body (Optional)

<Property name={"title"} type={"string"} required={true} deprecated={undefined}>

</Property>

<Property name={"post"} type={"string"} required={true} deprecated={undefined}>

</Property>

### Header Parameters

<Property name={"authorization"} type={"string"} required={true} deprecated={false}>

<span>Example:  `"Bearer UNKEY_API_KEY"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `201` | Create a post |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X POST "https://example.com/posts/v1/create/" \
  -H "authorization: Bearer UNKEY_API_KEY" \
  -d '{
  "title": "string",
  "post": "string"
}'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://example.com/posts/v1/create/", {
  method: "POST",
  headers: {
  "authorization": "Bearer UNKEY_API_KEY"
}
});
```

</Request>

</Requests>

<Responses items={["201","400","401","403","404","409","429","500"]}>

<Response value={"201"}>

<ResponseTypes>

<ExampleResponse>

```json
{}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/posts/v1/get/:id"}>

### Path Parameters

<Property name={"id"} type={"string"} required={true} deprecated={false}>

</Property>

### Header Parameters

<Property name={"authorization"} type={"string"} required={true} deprecated={false}>

<span>Example:  `"Bearer UNKEY_API_KEY"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Authorized Response |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X GET "https://example.com/posts/v1/get/:id" \
  -H "authorization: Bearer UNKEY_API_KEY"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://example.com/posts/v1/get/:id", {
  method: "GET",
  headers: {
  "authorization": "Bearer UNKEY_API_KEY"
}
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "id": 0,
  "title": "string",
  "post": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  id: number;
  title: string;
  post: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"PATCH"} route={"/posts/v1/update/:id"}>

### Request Body (Optional)

<Property name={"title"} type={"string"} required={true} deprecated={undefined}>

</Property>

<Property name={"post"} type={"string"} required={true} deprecated={undefined}>

</Property>

### Path Parameters

<Property name={"id"} type={"string"} required={true} deprecated={false}>

</Property>

### Header Parameters

<Property name={"authorization"} type={"string"} required={true} deprecated={false}>

<span>Example:  `"Bearer UNKEY_API_KEY"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Update post |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X PATCH "https://example.com/posts/v1/update/:id" \
  -H "authorization: Bearer UNKEY_API_KEY" \
  -d '{
  "title": "string",
  "post": "string"
}'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://example.com/posts/v1/update/:id", {
  method: "PATCH",
  headers: {
  "authorization": "Bearer UNKEY_API_KEY"
}
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "id": 0,
  "title": "string",
  "post": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  id: number;
  title: string;
  post: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"DELETE"} route={"/posts/v1/delete/:id"}>

### Path Parameters

<Property name={"id"} type={"number"} required={true} deprecated={false}>

</Property>

### Header Parameters

<Property name={"authorization"} type={"string"} required={true} deprecated={false}>

<span>Example:  `"Bearer UNKEY_API_KEY"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `201` | Delete Post |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X DELETE "https://example.com/posts/v1/delete/:id" \
  -H "authorization: Bearer UNKEY_API_KEY"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://example.com/posts/v1/delete/:id", {
  method: "DELETE",
  headers: {
  "authorization": "Bearer UNKEY_API_KEY"
}
});
```

</Request>

</Requests>

<Responses items={["201","400","401","403","404","409","429","500"]}>

<Response value={"201"}>

<ResponseTypes>

<ExampleResponse>

```json
{}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "message": "string",
  "docs": "string"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  message: string;
  docs: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

</Root>
